{% extends '::base.html.twig' %}

{% block content %}

    <h1>{{ genre.name }}</h1>

    {% if books %}

        {% include '::book-grid.html.twig' with { 'books': books } %}

    {% else %}

        <p>
            {% trans %}text.no_books{% endtrans %}
        </p>

    {% endif %}
{% endblock %}